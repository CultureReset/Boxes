[Unit]
Description=NODE shell window
PartOf=graphical-session.target
After=nodeos-daemon.service
Requires=nodeos-daemon.service

[Service]
Type=simple
ExecStart=/usr/bin/env nodeos shell
Restart=on-failure
RestartSec=1

[Install]
WantedBy=graphical-session.target
